# Y'World 迭代时间线

每一轮记录：**你几点提的需求 → 想达成什么效果 → 我改了什么 → 最终效果如何**。

> **时间说明**
> - 「提出」一列来自会话中的消息时间：带确切时间戳的按实际记录（如 02:37、04:37）；03:31 之后到 04:37 之间系统未注入独立时间戳，这几轮统一标注为 **03:35–04:30 之间**，不虚构精确分钟。
> - 「落盘」一列以文件实际修改时间为准：`ppt-campus-network.html` = 03:11，`index.html` 最后一次落盘 = 04:57（中间迭代未保留独立快照，故多数版本标注为「即时交付，代码合并至最终文件」）。
> - 每次改动后都跑过无头冒烟测试（`.workbuddy/smoke-test.js`），阶段数随功能增长：1 → 3 → 4 → 5 → 6 → 7 → 8。

---

## v1 · 星球初版诞生
**提出 02:37 ｜ 落盘 03:11**

- **你想达成的效果**：参考 Topology Field 那种线框球，做一个叫 y'world 的展示网站；球体划分成「我做过的项目」；悬停点像放大地图一样推近；悬停某个分区时整块高亮；拉出一根虚线接一段项目介绍；点击高亮区跳转到一个精美 PPT。
- **我改了什么**
  - `index.html` 从零搭建：Canvas 全屏渲染，`炽黄金角` 均布 170 个球面光点 + k 近邻连线生成三角网格
  - `projectPoint()`：绕 Y/X 轴旋转 + FOV 透视投影，输出屏幕坐标与深度
  - 花朵分区：极坐标花瓣方程生成轮廓，`flowerOutline()` 预计算每朵花的顶点
  - 悬停：命中检测 → `zoomTarget` 过渡到 1.9 → 整朵花色相提亮 + 发光；`drawLabel()` 画虚线 + 文字卡
  - `ppt-campus-network.html`：园区网项目演示页（封面/概览/两页占位），键盘 + 按钮 + 圆点三种导航
  - 「园区网建设项目（真机实施版）」用了你素材库里的真实内容：华为真机、VLAN/ACL、OSPF 多区域、NAT、无线与服务器区
- **效果**：可交互原型第一次跑通。冒烟测试首版就抓到一个真 bug —— `RADIUS` 声明为常量却在 `layout()` 里被重新赋值，已修。

> 中间穿插：你提供的新录屏是空文件（`C:\Users\24426\Videos\...\021036.mp4` 为 0 字节），你随后重新录制了 `20260924_030537.mp4`（Topology Field 组件，纯黑背景 + 青绿光点线框球）。我抽帧分析后确认：画面底部的蓝色圆圈和左边缘蓝框是录屏工具的 UI，不是设计的一部分。

---

## v2 · 时间线航线回放（航班线风格）
**提出 03:31（参考视频 `20260924_033115.mp4`）｜ 即时交付**

- **你想达成的效果**：像飞机航班线视频那样，沿时间线从早期项目飞到后期项目，路径用紫色。
- **我改了什么**
  - `regions` 增加 `year / era` 字段，`flightOrder` 按年份升序排列
  - `arcPoint()`：球面线性插值（slerp）+ `sin(πt)` 离地抬升 —— 起飞拱起、降落回落
  - `drawFlight()`：紫色 `#a855f7` 发光底线 + 亮线 + 彗尾渐隐拖尾 + 按航向旋转的纸飞机三角机头
  - 相机跟随：视角锁机头，`updateFlight()` 接管悬停与缩放
  - 途经花朵 `_bloom` 点亮；底部时间线 UI（渐变进度条 + 年份圆点 + 当前站标签）
- **效果**：一趟约 21 秒的飞越记录片。测试期间修掉一处航线段绘制时「已完成段/当前段」边界判断错误。

---

## v2.1 · 到站定格 + 飞行提速
**提出 03:35–04:30 之间 ｜ 即时交付**

- **你想达成的效果**：飞到每块区域时该区域就高亮并放大，停 2 秒；中间飞行速度提到原来的 1.25 倍。
- **我改了什么**：到站时 `zoomTarget=1.9` 锚定花心 + 整朵高亮 + 显示项目文字卡，`dwellDur` 1500→2000ms；`flyDur` 2400→1920ms（1.25×）。
- **附带修复**：主循环里的悬停缩放分支会在飞行途中覆盖 `updateFlight()` 的相机目标 —— 也就是说 v2 的「相机跟随」实际是失效的。重构为仅 `!flight.active` 时才走悬停分支后，镜头才真正跟着飞机走。

---

## v2.2 ~ v2.5 · 悬停抖动：来回四轮才找到根因

这段是整个项目里最值得复盘的部分 —— 表面上是一个「有点卡」的手感问题，实际挖了四层才到底。

### v2.2 回滞判定（治标）
**提出 03:35–04:30 之间**

- **你想达成的效果**：鼠标悬停小光点时太敏感，画面频繁闪烁。
- **我改了什么**：`pick()` 加回滞 + 粘滞 —— 进入半径 18→11px（要凑近才触发），释放需移出 28px（粘着不放）；光点悬停放大 1.25→1.16；镜头趋近时间常数 160→250ms。
- **效果**：减轻，但你说「还是有」。

### v2.3 悬停时完全停转（往根因走了一层）
**提出 03:35–04:30 之间**

- **你的判断**：因为地球还在转，所以才会卡 —— 这个判断是对的。
- **我改了什么**：悬停任何目标时，转速经缓动 `state._spin` 平滑降到 0（原来只是降到 6%），松开再平滑恢复。
- **效果**：进一步减轻，你说「还是识别不了选了哪个点」。

### v2.4 光标处缩放 v1 —— 我写错了公式，比原先更卡
**提出 03:35–04:30 之间**

- **我改了什么**：把花朵/光点的缩放锚点改成「光标所在的点」。
- **真实结果**：**负优化**。锚点语义是「该点会被推到屏幕中心」，我直接把光标下的点设为锚点，反而让它更剧烈地离开光标 —— 你反馈「还不如第一版」。

### v2.5 修正锚点公式 —— 真正解决
**提出 03:35–04:30 之间 ｜ 你确认「这一版可以了」**

- **我改了什么**：按目标倍率反推锚点
  ```
  a_target = a + (S − cam) · (1/z − 1/zt)
  ```
- **数学定点验证**：模拟 60 帧插值，光标下那个点的屏幕坐标全程最大漂移 **0.31px**（丢失悬停需移出 30px，差两个数量级）。目标在数学上钉死在光标下，不存在「缩放→滑走→缩回」的循环。
- **顺手补强**：进入半径放宽到 15px（既然不会再滑走就可以更灵敏）、释放 30px；选中光点加白色亮核 + 双层亮环 + 两级光晕，一眼能看出选的是哪一个。

---

## v2.6 · 滚轮改为调节自转速度
**提出 04:37 ｜ 即时交付**

- **你想达成的效果**：鼠标悬停在地球上，滚轮就能加快/减慢自转速度。
- **我改了什么**：移除原来的「滚轮微调缩放」整组逻辑（一个滚轮只干一件事），换成 `speedMul`（0=停转，1=默认，上限 6）；自转公式 `ry += 0.00008 · _spin · speedMul · dt`；左下角新增速度指示器。
- **v2.6.1 你的补充**：滚动调速时不要识别悬停，滚轮一停立刻恢复 —— 实现 300ms「滚动静默窗口」，期间跳过 `pick()` 并清空当前悬停，窗口一过下一帧立即恢复。

---

## v2.7 · 在两朵花之间手动选锚点创建项目
**提出 03:35–04:30 之间（在 v2.6 之后） ｜ 即时交付**

- **你想达成的效果**：两朵花之间空隙很小时，空隙里的光点识别不灵敏，希望能手动选一个锚点来新建项目。
- **根因**：花朵的命中判定带 26px 最小半径下限，把两朵花之间的缝隙整体「吃」掉了，光点永远轮不到判定。
- **我改了什么**
  - 花朵判定改用真实花瓣半径（下限 26→12px），并加优先级：光点若不在任何花瓣角半径内 → **光点优先**
  - 悬停空隙光点 → 气泡「＋ 点击创建新项目」→ 创建弹窗（名称必填 / 介绍 / 时间）
  - 由光点三维坐标反推经纬度，`rebuildFlight()` 让新项目自动插入时间线航线
  - localStorage 持久化（key `yworld.custom`）
- **测试抓到的真 bug**：新建的 region 缺 `r.outline`，`drawFlowers` 会崩 —— 补上 `flowerOutline(r)` 并给初始化处加了幂等判断。

---

## v2.8 · 速度改成可拖动滑杆
**提出 04:43 ｜ 即时交付**

- **你想达成的效果**：左下角速度显示改成能像进度条一样自己拖，0.5 一小格、1 一大格，不用一直滚滚轮。
- **我改了什么**：`#speedHud` 重构为常驻滑杆（轨道 + 填充 + 圆拖钮 + 刻度层：0.5 短刻度 / 整数长刻度带数字）；`pointerdown/move/up` + `setPointerCapture` 拖动，`Math.round(v*2)/2` 吸附 0.5；拖动与鼠标停留在 HUD 上时暂停 `pick()`。
- **效果**：点到/confirmed某格即刻生效；滚轮调速保留且与滑钮位置互相同步；无操作 1.6s 后降为 45% 半透明常驻，鼠标移入点亮。

---

## v2.9 · 访客可看、编辑仅站长
**提出 04:49 ｜ 即时交付**

- **你想达成的效果**：别人能访问但不能动里面的东西（增删项目、改 PPT 只能你操作），且进入不需要密码。
- **我改了什么**
  - 右上角 `#lockBtn`：默认 🔒 访客模式，点击弹密码窗，`djb2` 哈希比对（`PASS_HASH`，默认密码 `yworld`）
  - 门禁点：访客点空隙光点只给提示；悬停气泡文案随模式切换；新建/删除均有站长守卫（含 `openCreateModal` 内部双保险）
  - 新增删除能力：站长右键花朵 → 确认窗 → 移除 + 航线重排（正好补上「增删」里的「删」）
  - 站长状态存 `sessionStorage`，关标签页自动回到访客；可一键锁回
- **修复**：两个弹窗共用 `_modalOpen` 标志，Enter 会串到另一个弹窗的确认逻辑 —— 改为各自用自身 `classList` 判定。

---

## v2.10 · HTTPS 上线 · 只读镜像 · 项目数据可携带
**提出 04:54 / 04:57 ｜ 落盘 04:57–04:58**

- **你想达成的效果**：① 用更安全的 HTTPS；② 编辑只留在本地，线上给一个只读版；③ 换电脑还能继续编辑。
- **我改了什么**
  - 澄清：本地预览的 `http://127.0.0.1` 是本机回环不出网，HTTPS 只在公网发布时有意义
  - 「换电脑可编辑」的缺口：界面创建的项目原本只存在浏览器 localStorage，换机就丢 → 站长面板新增 **💾 导出 / 📥 导入项目数据**（JSON，含经纬度与花瓣样式，按「标题+纬度」去重）
  - `build-readonly.js`：由可编辑版生成只读镜像，五步转换（隐藏全部站长 UI / 移除站长会话恢复 / 关闭创建气泡 / 改底部文案 / 烘焙项目数据），每步替换都做唯一性断言 + 收尾安全自检
  - 冒烟测试支持 `node smoke-test.js <path>`，对两个版本各跑一遍
- **效果**：只读版 HTTPS 上线 https://0b21c030bdc345838a1f13e52f32a5e0.app.workbuddy.host ；旧的带编辑入口链接已下线。（⚠️ 该 CloudStudio 链接后来已被平台回收失效，现用 v2.13 的 GitHub Pages 固定地址。）

---

## v2.11 · 作为独立作品集入库
**提出 14:51**

- **你想达成的效果**：把每一次变化较大的代码和作用整理成自己的作品集仓库，推送到 Gitee 与 GitHub。
- **我改了什么**：新增 `README.md`（作品定位、功能地图、技术要点、运行与发布说明）与本文件 `CHANGELOG.md`（逐轮时间线）；`.gitignore` 排除构建产物与项目素材；按「站点源码 / 构建脚本 / 文档」三段式提交。
- **说明**：中间迭代没有保留独立文件快照，因此提交历史是三段式结构，逐轮改动细节完整保留在本文件里（这也是真实情况 —— 当时的环境没有版本管理）。

---

## v2.12 · 阅读模式光点不再识别
**提出 09-26（补齐 09-24 遗留）｜ 落盘 09-26**

- **你想达成的效果**：阅读（访客）模式下，鼠标移到缝隙光点（拓扑网散点，原本用于站长"锚点建项目"）应当完全不被识别——此前访客悬停光点还会弹出"🔒 仅站长可创建"气泡、变 crosshair 光标并高亮成白核，阅读体验有干扰。
- **我改了什么**
  - `index.html` 的 `pick()`：仅当 `state.ownerMode === true` 才执行光点（gap node）判定与粘滞释放；访客模式直接 `hoverNode = -1` 并跳过光点循环。
  - 效果：访客模式悬停光点无任何反应（无气泡 / 不变光标 / 不高亮），点击也不触发创建提示；花朵分区的高亮、虚线标签、点击进 PPT 两种模式都保留。
  - 重建只读镜像：`node build-readonly.js` 成功（只读版强制 `ownerMode=false`，安全自检通过），线上镜像同步生效。
- **说明**：未改动 click 处理，访客模式 `hoverNode` 恒为 -1，光点分支自然不触发。

---

### 附：验证方式

```bash
node .workbuddy/smoke-test.js                                  # 可编辑版
node .workbuddy/smoke-test.js D:/y'world/yworld-site-public/index.html   # 只读版
```

八个阶段：常规渲染 120 帧 → 航线回放全程 → 回放后复位 → 滚轮调速 → 滚动静默/恢复 → 锚点创建项目 → 滑杆拖动吸附 → 访客/站长门禁。

---

## v2.13 · 线上地址迁移到 GitHub Pages（固定域名）
**提出 09-26 02:40**

- **你想达成的效果**：CloudStudio 的临时链接被平台回收后 404，需要一个永不失效、链接里含 "yworld" 的正式对外地址。
- **我改了什么**
  - 用 GitHub PAT 在 `shmily-lab` 下新建公开仓库 `yworld`，推送发布版 `yworld-site-public/` 并开启 GitHub Pages（`main` 分支根目录），加 `.nojekyll` 纯静态托管；
  - `README.md`：在线预览地址改为 **https://shmily-lab.github.io/yworld/**，并修正 GitHub 源码仓库链接（去掉仓库名多余的 `-` 前缀）；
  - 本文件 v2.10 条目中已失效的 CloudStudio 链接标注"已失效，改用 v2.13 固定地址"。
- **效果**：固定地址 https://shmily-lab.github.io/yworld/ 上线（HTTP 200，页面为还原版：深色背景、无银河）。CloudStudio 旧链接（0b21c030… / d66dab5a…）全部弃用。此后重新发布地址不再变化。

---

## v2.14 · 进站握手动画：像素女孩场景
**提出 09-26 02:59**

- **想要的效果**：进入星球前，画面里有一位戴眼镜的像素女孩坐在电脑前敲键盘，桌上有电脑/桌子/椅子/键盘；电脑屏幕实时显示一行行指令（三次握手日志），连接建立成功的瞬间屏幕由红转绿，女孩举手欢呼并发出欢呼音效。
- **已实现**
  - 握手覆盖层新增 `#hsGirl` 像素画布（380×240，image-rendering: pixelated）：女孩（戴青色眼镜、马尾后发）、椅子、桌子、键盘、显示器；女孩持续敲键盘（手随节奏上下），屏幕逐行打印 `> SYN / SYN-ACK / ACK / CONNECTED`；
  - 屏幕底色在连接中保持**红**（rgb(60,12,12)），`ESTABLISHED` 时平滑过渡到**绿**（rgb(13,58,28)），边框同步变色；
  - 成功瞬间女孩双手举起 + 轻微弹跳 + 头顶冒出 ♪/★，并播放新增的 `sfx.cheer()`（快速上行琶音 + 亮闪尾音）；
  - 移动端加 `@media` 收敛画布与标题/日志尺寸。
- **同步**：已双端提交推送（GitHub `shmily-lab/YWorld-AI-Coding` + Gitee `jysxh_code/YWorld-AI-Coding`），并更新 GitHub Pages 线上站点到本版。

## v2.15 · 握手场景换角色：像素男孩
**提出 09-26 03:34**

- **想要的效果**：把进站握手画面里的像素女孩，换成参考图中的角色——奶白色蓬松头发 + 呆毛、蓝色方眼、腮红、米白西装配浅蓝衬衫和蓝色领结的像素男孩。
- **我改了什么**
  - `drawGirl()` 角色部分整体重绘：去掉眼镜与紫发，新配色 SKIN/HAIR/SUIT/SHIRT/BOW 等常量；呆毛随敲键盘节奏轻摆；
  - 眼睛改为蓝色方眼 + 高光 + 深蓝底边，腮红加大到 2×2 更明显；
  - 画布黑底上新增 7 颗随时间闪烁的十字小星星，呼应参考图氛围；
  - 敲键盘、屏幕红→绿、举手欢呼 + ♪/★、音效等全部逻辑保持不变；
  - 用无头浏览器定格验证了待机帧与欢呼帧，验证后移除临时 `#test-cheer` 调试锚点。
- **最终效果**：待机时奶发男孩低头敲键盘、呆毛轻摆、星星闪烁；连接成功屏幕转绿，男孩举手欢呼弹跳。线上与本地一致。

## v2.16 · 握手场景接入豆包像素动画视频（TCP 三次握手结合版）
**提出 09-26 21:20**

- **想要的效果**：把豆包生成的「8秒小人敲键盘」动画视频原样嵌入握手场景（人物/动画/画风零改动），TCP 三次握手动画画在小人旁边且画风一致，8 秒握手建立成功，音效直接用视频里的。
- **我改了什么**
  - 视频逐帧分析确定剧情时间线：0~2.9s 敲键盘红屏代码 → ~3s 屏幕变绿✓ → 6s 起举手欢呼 → 8.06s 结束；
  - 视频**原字节 base64 内嵌** index.html（582KB → 776KB，不转码不损画质），`<video>` 元素随「▶ 建立连接」点击播放——自带 AAC 音轨即握手音效，点击手势天然满足浏览器出声策略；
  - 新增 `#hsTCP` 像素动画画布（360×252，6px 格）画在视频右侧：本机像素 CRT 电脑（红代码滚动 → 建立后绿✓）、y'world 条纹自转像素星球（建立后亮光环）、虚线链路 → 绿色实线+流动光点、弧线飞行数据包（SYN/SYN-ACK/ACK 发光方块+标签）、背景闪烁星星，配色全部取自视频（米白/红代码/绿✓/浅蓝）；
  - 时间轴对齐视频剧情：0.45s SYN → 1.4s SYN-ACK → 2.35s ACK → **3.15s ESTABLISHED**（正好视频屏幕变绿✓）→ 8.05s 淡出进星球；`video.ended` 事件兜底触发进站；
  - 移除旧 Canvas 手绘像素男孩（drawGirl 等 ~90 行）与 sfx 合成信令音调用（音效全用视频的）；清理 v1 节点图残留 CSS；
  - 移动端 ≤900px 视频与 TCP 画布上下堆叠。
- **最终效果**：进入星球前，左边像素小人敲键盘（红屏→绿✓→欢呼），右边像素电脑与星球之间三次握手飞包，屏幕变绿瞬间即连接建立，8 秒后自动进站；全程音效来自视频。
