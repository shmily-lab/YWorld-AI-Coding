# 数据库学习工作台 · 每日复习清单

> 生成时间：2026-09-26 21:00（北京时间）
> 数据来源：`python run.py review` + `python run.py diagnose`

---

## 一、今日到期题目清单

**今日无到期复习任务。** 🎉

复习队列为空。间隔复习规则：新做的题 1 天后进入队列，答对则间隔逐级拉长（1 / 2 / 4 / 7 / 15 / 30 / 60 天）。

| 题号 | 标题 | 复习原因 |
|------|------|----------|
| —— | 无到期题目 | 尚未产生学习记录，复习队列为空 |

> 想立即开做：`python run.py review --do`

---

## 二、错题本未攻克的题

**当前错题本 0 题未攻克。**

| 题号 | 标题 | 最近一次错误原因 |
|------|------|------------------|
| —— | 无 | 尚无答题记录，未产生错题 |

说明：错题本记录的是"做过但没通过"的题。目前进度文件 `progress/progress.json` 中 `questions` 为空，即一题未练，所以既没有到期任务，也没有错题。

---

## 三、各知识点掌握度

**总体：已练 0 题，通过 0 题；平均每题尝试 0 次；一次做对率 0%。**

全部 56 个知识点均为「未练习」状态，系统未识别出明显短板（因为没有数据可供判断）。

按阶段归类的知识点覆盖情况：

| 阶段 | 主题 | 知识点数 | 状态 |
|------|------|----------|------|
| 1 | 查询基础 | 基础过滤、排序分页、NULL 语义、DISTINCT、聚合、字符串函数、索引失效、日期处理、CASE 表达式 | 未练习 |
| 2 | 聚合与分组 | GROUP BY、HAVING、条件聚合、派生表 | 未练习 |
| 3 | 多表连接 | JOIN 基础、LEFT JOIN、反连接、派生列、自连接、行数膨胀 | 未练习 |
| 4 | 子查询 | 标量子查询、EXISTS、去重、关联子查询 | 未练习 |
| 5 | CTE 与窗口函数 | CTE、递归CTE、窗口函数、排名函数、LAG/LEAD、窗口帧 | 未练习 |
| 6 | 数据变更 | INSERT、外键、UPDATE、DELETE、主子表顺序、UPSERT、唯一约束、事务、原子性、数据归档、INSERT SELECT | 未练习 |
| 7 | 表设计与约束 | 表设计、约束、幂等、数据类型、规范化 | 未练习 |
| 8 | 索引 | 单列索引、执行计划、联合索引、覆盖索引、排序优化、冗余索引 | 未练习 |
| 9 | 调优实战 | 深分页、COUNT 优化、查询改写、TOP-N | 未练习 |

> 掌握度要跑出真实数据，得先做题。诊断结论目前是"无短板"而非"已掌握"。

---

## 四、建议重点训练的题号

当前没有到期任务和错题，因此本日以**建立基线**为目标：先做阶段 1，让诊断系统拿到第一批数据。

### 今日抽测题（随机）

| 题号 | 标题 | 阶段 | 知识点 | 难度 / 分值 |
|------|------|------|--------|-------------|
| **S1-Q02** | DISTINCT 去重的真实含义 | 阶段 1 · 查询基础 | NULL 语义、DISTINCT | 入门 / 5 分 |

**训练方向提示（不含答案）：**
- `DISTINCT` 作用于**整个 SELECT 列表的组合**，不是只作用于紧跟它的那一列——单列查询时这一点容易被忽略。
- 排除空值要用 `IS NOT NULL`，**不要**写 `city != NULL`（结果永远是 UNKNOWN，一行都筛不出来）。
- 题目要求按 `city` 升序、列名就叫 `city`。

> 建议先独立完成，再对答案：`python run.py check S1-Q02 "<你的SQL>"`
> 需要看完整题干：`python run.py show S1-Q02`

### 后续建议顺序

1. **S1-Q01 → S1-Q08**：阶段 1 全部 8 题，覆盖查询基础，先把基准数据跑出来。
2. **S2-Q01、S2-Q03、S2-Q05**：阶段 2 的分组核心三题（含 0 单客户、WHERE/HAVING 分工、SELECT 与 GROUP BY 对齐）。
3. **S3-Q01 起**：JOIN 基础，进入多表阶段。

---

## 五、融合题（阶段 10）解锁状态

**阶段 10 全部 6 道融合题均为 🔒 锁定状态。**

解锁规则：融合题 `requires` 列出的前置阶段**全部通过**后才解锁。

| 题号 | 标题 | 状态 |
|------|------|------|
| S10-Q01 | 融合复习①：客户结构体检 | 🔒 未解锁 |
| S10-Q02 | 融合复习②：城市维度经营看板 | 🔒 未解锁 |
| S10-Q03 | 融合复习③：商品动销分析 | 🔒 未解锁 |
| S10-Q04 | 融合复习④：客户价值分层（RFM 雏形） | 🔒 未解锁 |
| S10-Q05 | 融合复习⑤：可交付的历史数据归档 | 🔒 未解锁 |
| S10-Q06 | 融合复习⑥：给分页接口配一套索引 | 🔒 未解锁 |

距离最近一次解锁（至少通关阶段 1）：还需完成阶段 1 的 8 题。

---

## 附：常用命令

```bash
python run.py review              # 查看今日复习队列
python run.py review --do         # 立即开始今日复习
python run.py diagnose            # 学情诊断 + 知识点掌握度
python run.py list --stage 1      # 列出某阶段题目
python run.py show S1-Q02         # 查看题干
python run.py check S1-Q02 "SQL"  # 提交答案判分
python run.py wrong               # 查看错题本
```

---

*本清单由每日自动化任务生成，明日 21:00 更新。*
